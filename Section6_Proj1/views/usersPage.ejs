<%- include('includes/head.ejs') %>

<h1>User Details</h1>

<% if(users.length > 0){ %>
    <% for(let data of users){ %>
    <ul>
        <li><%= data.name %></li>
    </ul>
    <% } %>

<% }else{ %>
    <h3>Opps, No user is available</h3>
<% } %>

</main>
</body>
</html>
